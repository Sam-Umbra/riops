[package]
name = "riops"
version = "0.1.6"
edition = "2024"
description = "Rust Input/Output Parallel Searching, a cmd tool that uses multi-threading to search for matches in files"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
rayon = "1.12.0"
walkdir = "2.5.0"
